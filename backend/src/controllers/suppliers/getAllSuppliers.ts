import type { Request , Response } from "express";
import { asyncHandler } from "../../utils/ayncWrapper.js";

const getAllSupplires = asyncHandler(async(req:Request , res: Response)=>{


})

export default getAllSupplires;